import 불러올 함수or클래스 from 경로(파일확장자 js생략가능)
export default 내보낼 함수or클래스

클래스형 컴포넌트는 render(){ 내용 } 로 그려준다

div와 같은 요소에 클래스를 주려면 class가 아니라 className="클래스명"을 사용
jsx에서는 if문 대신 조건부 연산자 - 삼항연산자를 사용한다

import component from './component'
<component>텍스트</component>
위와같이 <component></component>안의 값은 props.children으로 받아온다

component.defaultProps = {
	name : '기본이름'
	age : 20,
	children: 'aaa'
}
위와같이 defaultProps로 클래스의 디폴드값을 지정한다.

클래스형 컴포넌트에서 constructor 작성시 super(props) 필수로 작성

클래스형 컴포넌트는 setState로 state값을 변경한다 onClick={()=>{ this.setState({number : number+1},()=>{}) }}
함수형 컴포넌트는 useState로 state값을 변경한다. - [디폴드값의 이름, 변경할 값의 이름] = useState('초기값'); 
						const onClickEnter = ()=>변경할값의 이름('변경할 값');

